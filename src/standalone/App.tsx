import{useState}from'react';import{WeeklyReviewWorkspace}from'../components/WeeklyReviewWorkspace';import'../styles.css';export default function App(){const[locale,setLocale]=useState<'fa'|'en'>('fa');return <><button style={{position:'fixed',top:10,right:10,zIndex:10}} onClick={()=>setLocale(locale==='fa'?'en':'fa')}>{locale==='fa'?'EN':'FA'}</button><WeeklyReviewWorkspace locale={locale} direction={locale==='fa'?'rtl':'ltr'}/></>}
